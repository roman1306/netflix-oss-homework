version = '0.0.1'

mainClassName = "com.epam.hw.netflix.EmployeesApplication"

dependencies {
    compile project(':commons')
    compile libpacks.spring_common
    compile libpacks.cloud_common
    compile libpacks.cloud_config_client
    compile libpacks.groovy
}